"""SENTINEL-4: a multi-agent countermeasure unit against the rogue AI "The Entity".

Sub-packages:
    agents: The four domain specialists plus the standard-defence module.
    tools:  The five custom tools and the registry that resolves them.

Top-level modules: ``config``, ``state``, ``llm_client``, ``router``, ``graph``,
``evaluation`` and ``fallback``.
"""
